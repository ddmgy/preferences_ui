name: preferences_ui_example
version: 0.3.1
homepage: https://github.com/ddmgy/preferences_ui
author: David Mougey <imapiekindaguy@gmail.com>

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: "^6.0.0"
  flutter_markdown: "^0.6.0"
  shared_preferences: "^2.0.0"
  preferences_ui:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  preferences_ui:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - CHANGELOG.md
